<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xzsd.pc.menu成功.dao.MenuDao">


    <!--    查询菜单新增  失败-->
    <insert id="menuAdd" parameterType="com.xzsd.pc.menu成功.entity.MenuData">
       insert into menu
       (
       menu_id,
       menu_name,
       menu_path,
       menu_comment,
       is_delete,
       create_by,
       create_time,
       update_by,
       update_time,
       version
       )
       values(
       #{menuId},
       #{menuName},
       #{menuPath},
       #{menuComment},
       #{isDelete},
        #{createBy},
        now(),
        #{updateBy},
        now(),
         0
       )
    </insert>

    <!--统计是否存在相同的菜单名-->
    <select id="countMenuName" parameterType="com.xzsd.pc.menu成功.entity.MenuData" resultType="int">
        select count(menu_name)
        from menu
        where menu_name = #{menuName}
        and is_delete = 0
    </select>

    <!--统计是否存在相同的菜单路由-->
    <select id="countMenuUrl" parameterType="com.xzsd.pc.menu成功.entity.MenuData" resultType="int">
        select count(menu_path)
        from menu
        where menu_path = #{menuPath}
        and is_delete = 0
    </select>
    <!--    查询菜单详情  -->
    <select id="getMenuByMenuId" parameterType="java.lang.String" resultType="com.xzsd.pc.menu成功.entity.MenuData">
       select
            menu_id menuId,
            menu_name menuName,
            menu_path menuPath,
            menu_comment menuComment,
            version
        from menu
        where is_delete = 0
         AND menu_id = #{menuId}
    </select>

    <!--    修改菜单  参数menuName,menuId,menuComment,menuPath,version = 0   版本号必须要有 -->
    <update id="menuUpdate" parameterType="com.xzsd.pc.menu成功.entity.MenuData">
        update menu
        set
            menu_name = #{menuName},
            menu_path = #{menuPath},
            menu_comment = #{menuComment},

            update_time = now(),
            version = version + 1
        where is_delete = 0
        and version = #{version}
        and menu_id = #{menuId}
    </update>

       <!--    查询菜单列表 无参数-->
    <select id="menuListCheck" parameterType="com.xzsd.pc.menu成功.entity.MenuData" resultType="com.xzsd.pc.menu成功.entity.MenuData">
        select
            menu_id,
            menu_name,
            menu_comment,
            version
        from menu
        where is_delete = 0
        order by create_time asc
    </select>

    <!--    删除菜单  输入ID  -->
    <update id="menuDelete" parameterType="com.xzsd.pc.menu成功.entity.MenuData">
        update menu
        set
        is_delete = 1,
        update_time = now(),
        update_by = #{currentUserId},
        version = version + 1
        where menu_id = #{menuId}
    </update>

    <!--根据角色查询首页菜单显示的功能-->
    <select id="getPageHomeMenu" parameterType="java.lang.String" resultType="com.xzsd.pc.menu成功.entity.MenuData">
        select
        menu_id as menuId,
        menu_name as menuName
        from menu
        where is_delete = 0
        <if test="role == 2">
            and is_shop_manager = 1
        </if>
    </select>

</mapper>